<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".fragments.viewPager.expense.FirstScreen2">
    <ProgressBar
        android:id="@+id/progressBar_unfocused"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:indeterminateOnly="false"
        app:layout_constraintTop_toTopOf="parent"
        android:paddingTop="150dp"
        app:layout_constrainedHeight="true"
        android:progressDrawable="@drawable/indicator_unfocused"
        android:progress="50"
        app:layout_constraintBottom_toBottomOf="parent" />
    <ProgressBar
        android:id="@+id/progressBar"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:indeterminateOnly="false"
        app:layout_constraintTop_toTopOf="parent"
        android:paddingTop="150dp"
        app:layout_constrainedHeight="true"
        android:progressDrawable="@drawable/indicator"
        android:progress="50"
        app:layout_constraintBottom_toBottomOf="parent" />

    <TextView
        android:id="@+id/indicator"
        style="@style/TextAppearance.AppCompat.Large"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="casual"
        android:textColor="@color/blue_700"
        android:textStyle="bold"
        android:textSize="32sp"
        tools:text="100"
        app:layout_constrainedHeight="true"
        app:layout_constrainedWidth="true"
        app:layout_constraintBottom_toBottomOf="@id/progressBar"
        app:layout_constraintEnd_toEndOf="@id/progressBar"
        app:layout_constraintStart_toStartOf="@id/progressBar"
        app:layout_constraintTop_toTopOf="@id/progressBar"
        app:layout_constraintVertical_bias="0.55"/>
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/the_amount_spent_from_the_salary"
        app:layout_constraintStart_toStartOf="@id/progressBar"
        app:layout_constraintEnd_toEndOf="@id/progressBar"
        android:textStyle="bold"
        android:textColor="@color/black"
        android:fontFamily="@font/crafty_girls"
        android:textSize="11sp"
        app:layout_constraintTop_toBottomOf="@id/indicator"/>
</androidx.constraintlayout.widget.ConstraintLayout>